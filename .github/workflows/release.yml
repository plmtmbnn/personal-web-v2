- name: Release
        run: npx semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # If you also publish to NPM, you'd add NPM_TOKEN: ${{ secrets.NPM_TOKEN }}